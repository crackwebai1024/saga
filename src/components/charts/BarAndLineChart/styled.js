import { inlineStylesBase } from '../styled';

export * from '../styled';

export const inlineStyles = {
  ...inlineStylesBase,
  lineChartStyle: {
    data: { stroke: '#054478', strokeWidth: 1 },
  },
};
