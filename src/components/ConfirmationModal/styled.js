import styled from 'styled-components';

export const Wrapper = styled.div`
  padding: 20px;
`;

export const Title = styled.span`
  font-size: 24px;
  font-weight: bold;
  display: inline-block;
`;

export const TitleTargets = styled.span`
  font-size: 20px;
  font-weight: bold;
  display: inline-block;
`;
